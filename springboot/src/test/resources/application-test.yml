spring:
  datasource:
    url: jdbc:h2:mem:meer-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  flyway:
    enabled: false

security:
  disableAuth: false
  requireAppHeader: false
  jwt:
    secret: test-secret-please-change-test-secret-1234567890
