# Postgres connection
DB_HOST=
DB_PORT=
DB_NAME=
DEVDB_NAME=
DB_USER=
DB_PASSWORD=

# For local-db profile override the user/pass if different
# DB_USER=
# DB_PASSWORD=

# Firebase Admin (pick one)
FIREBASE_SERVICE_ACCOUNT=
GOOGLE_APPLICATION_CREDENTIALS=

# Security flags
SECURITY_REQUIRE_APP_HEADER=true
SECURITY_REQUIRE_APP_CHECK=true
SECURITY_DISABLE_AUTH=false

# JWT
SECURITY_JWT_SECRET=change-me-please-change-me-please-change-me-123456789012345678901234
SECURITY_JWT_ACCESS_TTL_MINUTES=60
SECURITY_JWT_REFRESH_TTL_DAYS=7

# Google OAuth client IDs (per platform)
GOOGLE_ANDROID_CLIENT_ID=
GOOGLE_IOS_CLIENT_ID=
GOOGLE_WEB_CLIENT_ID=

# Optional: explicitly set profile
# SPRING_PROFILES_ACTIVE=default
